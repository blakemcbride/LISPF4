
This directory contains a pre-built lispf4 for the Mac.

It is a 64 bit ARM (Apple Silicon) binary.  It will not run on an
Intel Mac unless Rosetta is available; on an Intel Mac, build from
source instead with:  make

Usage:  ./lispf4 basic.img

To exit, use:  (exit)
